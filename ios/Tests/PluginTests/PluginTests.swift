import XCTest

// Placeholder test target. The plugin itself is exercised end-to-end from the host
// Tauri app on a real iOS device (Network + BLE transports).
final class PluginTests: XCTestCase {
  func testPlaceholder() throws {
    XCTAssertTrue(true)
  }
}
